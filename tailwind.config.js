/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        // Re-mapping gravity colors to a monochrome scale for the light theme
        gravity: {
          950: '#ffffff', // Background is now white
          900: '#f9fafb', // Very light gray for sections
          800: '#f3f4f6', // Card backgrounds
          700: '#e5e7eb', // Borders
          600: '#d1d5db',
          500: '#000000', // Primary Brand Color (Black)
          400: '#1f2937', // Secondary Text
          100: '#111827', // Main Text (Dark)
        },
      },
      fontFamily: {
        sans: ['Inter', 'sans-serif'],
      },
      animation: {
        'blob': 'blob 7s infinite',
        'fade-in-up': 'fadeInUp 0.8s ease-out forwards',
      },
      keyframes: {
        blob: {
          '0%': { transform: 'translate(0px, 0px) scale(1)' },
          '33%': { transform: 'translate(30px, -50px) scale(1.1)' },
          '66%': { transform: 'translate(-20px, 20px) scale(0.9)' },
          '100%': { transform: 'translate(0px, 0px) scale(1)' },
        },
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(20px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        }
      }
    },
  },
  plugins: [],
}
